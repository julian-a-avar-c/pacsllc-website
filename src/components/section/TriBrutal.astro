---
import Halfbleed from "@/components/section/Halfbleed.astro";
import { cn } from "@/lib/utils";

type Props = {
  class?: string;
};
---

<Halfbleed
  class={cn(
    "col-[fullbleed]",
    "text-white text-xl/9 sm:text-2xl/9",
    "pt-16 lg:pt-24 2xl:pt-32",
    )}
>
  <slot name="first" />
  <!-- <img
    src="/images/ee403138-dbe8-4cbe-aa61-dd9566177524.jpeg"
    class={cn(
      "col-[fullbleed] xl:col-[leftbleed] xl:row-[1/3]",
      "aspect-[2/1] xl:aspect-[none]",
      "xl:h-full",
      "object-[50%_100%] 2xl:object-[50%_20%] bg-fixed object-cover w-full",
    ]}
  /> -->

  <div
    class={cn(
      "col-[fullbleed] lg:col-[leftbleed] xl:col-[rightbleed]",
      "grid grid-cols-subgrid",
      "bg-zinc-800",
      "py-16",
      "xl:px-16",
      "lg:pr-16 xl:pr-0",
    )}
  >
    <div
      class={cn(
        "col-[fullbleed] sm:col-[content] lg:col-[content-left] xl:col-[content-right]",
        "flex flex-col gap-8",
        "px-4 sm:px-0",
      )}
    >
      <slot name="second" />
      <!-- 42px ~<= 5xl -->
      <!-- <h1 class="text-[42px] sm:text-6xl">PACS Mission</h1>
      <p class="">
        Our solutions are proven and designed based on the best practices in the
        fine art of engineering and are backed by our long experience in field
        work. We build our systems in a modular fashion and deliver them fully
        factory tested prior to shipping for rapid commissioning. Our turnkey
        modular approach results in successful quick implementation with an
        efficient high rate of return on investment.
      </p> -->
    </div>
  </div>

  <div
    class={cn(
      "col-[fullbleed] lg:col-[rightbleed]",
      "grid grid-cols-subgrid",
      "bg-[#5b4f43]",
      "py-16",
      "lg:pl-16 xl:px-16",
    )}
  >
    <div
      class={cn(
        "col-[fullbleed] sm:col-[content] lg:col-[content-right]",
        "flex flex-col gap-8",
        "px-4 sm:px-0",
      )}
    >
      <slot name="third" />
      <!-- 42px ~<= 5xl -->
      <!-- <h1 class="text-[42px] sm:text-6xl">PACS Vision</h1>
      <p class="">
        PACS understands that having a single source of project support
        simplifies and expedites decision-making, coordination, and execution.
        PACS consolidates responsibilities for engineering, procurement,
        installation inspection and start-up assistance, and can provide turnkey
        blending and manufacturing solutions. From the outskirts of New York
        City we serve the world.
      </p> -->
    </div>
  </div>
</Halfbleed>
